#include <iostream>
#include <string>
#include <vector>
#include <cmath>

using namespace std;


int judge(int n) {
    vector<int> nums;
    int sqrt_n = sqrt(n);
    
}

int main() {
    int L, K;
    cin >> L;
    cin >> K;
    string N;
    if (N.length() < K) {
        cout << 404;
        return 0;
    }

    return 0;
}
